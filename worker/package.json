{
  "name": "kagitaba-kms-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.8",
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9",
    "wrangler": "4.69.0"
  }
}
